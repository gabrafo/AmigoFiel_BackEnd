spring.application.name=amigofiel
spring.datasource.driver-class-name=org.postgresql.Driver

# Para executar em containers Docker
#spring.jpa.hibernate.ddl-auto=update
#spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:5432/amigosfieis
#spring.datasource.username=admin
#spring.datasource.password=145
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


# Para executar em sistema de gerenciamento de banco de dados

Conexao com banco de dados
spring.datasource.url=${db_url}
spring.datasource.username=${db_username}
spring.datasource.password=${db_password}
spring.jpa.show-sql=true
spring.jpa.open-in-view=true

logging.level.org.springframework.security=DEBUG


#Configuracao do Hibernate que decide o que fazer com as tabelas ao iniciar a aplicacao
#spring.jpa.hibernate.ddl-auto=create-drop